query GetTasks {
  tasks {
    id
    title
    description
    completed
    status
  }
}

query GetTask($id: ID!) {
  task(id: $id) {
    id
    title
    description
    completed
    status
  }
}
